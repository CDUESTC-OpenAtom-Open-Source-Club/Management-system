# AGENTS.md — 项目速览（给 AI Agent 用）

开放原子开源社团秘书处综合管理系统。**Java 17 · Spring Boot 3.2.5 · PostgreSQL 17 · Flyway · React 18 · TypeScript · Vite 5 · Ant Design 5**。

## 启动

```cmd
# 后端（8080）
set JAVA_HOME=E:\tooks\JAVA-JDK17.0.6\jdk-17.0.6 && E:\tooks\apache-maven-3.9.6\bin\mvn.cmd spring-boot:run

# 前端（5173，Vite proxy → 8080）
cd frontend && npm run dev
```

PG: `localhost:5432/openatom_club`，用户 `openatom/openatom123`（Docker Compose）。测试账号：`lintao/123456`（副会长全权限）、`LT/123456`（普通社员）。

## 核心架构决策

### 权限：无 role 表，基于 position + department 动态计算

```java
boolean fullAccess = "会长".equals(position) || "副会长".equals(position) || "秘书处".equals(department);
```

会长/副会长/秘书处 → 全部 17 个页面；普通成员 → 仅基础页面。`PermissionChecker.java` 后端校验，`utils/permission.ts` 前端控制 UI。**不要新增 role 表。**

**组织身份 = 届次 + 部门 + 职务**：届次只能由 fullAccess 管理员在「成员管理」修改；部门/职务可在「我的资料」自改，但后端禁止自设为管理员身份（秘书处/会长/副会长/部长，杜绝自提权）。

### 认证：JWT + ThreadLocal ActorContext

请求 → `JwtAuthenticationInterceptor` 解析 `Authorization: Bearer <token>` → 查 users 表 → 通过 member_id 查 members 表 → 构造 `ActorContext`（name/department/position/cohortId/cohortYear/userId）→ 存入 `ActorHolder`（ThreadLocal）→ 请求结束自动清理。白名单：`/api/auth/login`, `/swagger-ui`, `/v3/api-docs`, `/h2-console`, `/error`。JWT 密钥在 `application.yml`，168 小时过期，HMAC-SHA256。密码 BCrypt 加密，最少 6 位。

### 数据库：软删除 + Flyway + BIGINT IDENTITY + 届次

18 张核心表，全部 `deleted_at` 软删除（除 operation_logs/login_logs），JPA `@SQLRestriction("deleted_at IS NULL")`。主键统一 `BIGINT GENERATED BY DEFAULT AS IDENTITY`。`ddl-auto: validate`，DDL 全由 Flyway 管理（V1~V9）。

**届次（cohort）**是一级数据归属维度：`cohorts` 主表 + `members.cohort_id` / `homework_assignments.cohort_id`（可空=未分届）+ `archive_cohorts` / `point_item_cohorts` 两张关系表。历史成员/作业初始为未分届，不擅自假设为 2025 届。

### 文件机制：原生 fetch，禁用 axios

**这是关键陷阱。**`download.ts` 的 `downloadFile()` / `viewFile()` 必须用原生 `fetch`。axios 拦截器假设所有响应是 JSON 并检查 `code === 0`，Blob 响应没有 code 字段会被误判失败。后端存储 `./data/openatom-storage/{module}/{subDir}/UUID.ext`，`FileStorageService` 统一管理。

### API 规范

统一 `ApiResponse<T>` 包装（code=0 成功），分页用 `PageResult<T>`（list/total/page/size）。前端 axios 拦截器自动解包 `res.data`。异常：BizException→200，PermissionDenied→403，IllegalArgument→400，Exception→500。

### 操作日志

`OperationLogService.log(moduleName, actionType, targetId, description)` — 在 Service 层敏感操作后调用，写入 `operation_logs` 表。写入失败不阻断主流程。审计数据不软删除。

### 前端视觉：双风格隔离

- **登录页**：深蓝宇宙星空 + Canvas 动态星场 + 鼠标排斥扰动（`Login.tsx`, `LoginStarfield.tsx`）— 刻意设计
- **后台页面**：白色/浅灰 Ant Design 标准组件 — **禁止**玻璃拟态、霓虹光效、粒子特效

### 前端路由（17 个页面）

| 路由 | 页面 | 权限 |
|------|------|------|
| `/login` | 登录 | 公开 |
| `/` | Dashboard | 登录 |
| `/members` | 成员管理 | fullAccess |
| `/cohorts` | 届次管理 | fullAccess |
| `/point-items` | 积分项目管理 | fullAccess |
| `/my-applications` | 我的活动登记 | 登录 |
| `/point-applications` | 积分审核 | fullAccess |
| `/points-table` | 积分总表 | 登录 |
| `/my-homework` | 我的作业 | 登录 |
| `/homework-review` | 作业批改 | canManageHomework |
| `/homework-management` | 作业管理 | canManageHomework |
| `/archive-links` | 活动资料归档 | 登录 |
| `/meeting-minutes` | 会议纪要 | 登录 |
| `/finance` | 财务台账 | fullAccess |
| `/operation-logs` | 操作日志 | fullAccess |
| `/users` | 账号管理 | fullAccess |
| `/my-profile` | 我的资料 | 登录 |

## 关键陷阱速查

1. **文件下载/查看不能用 axios** ← 最重要
2. `GET /api/point-items` 返回 `PointItem[]`（非分页），不要用 `.list`
3. 积分总表搜索中文需 URL 编码
4. `point_records.point_item_id` 可为 NULL（手动加分），统计用 COALESCE
5. 不能禁用/删除自己的账号
6. 系统不内置默认账号（`DefaultAdminInitializer` 已移除），需手动创建账号
7. 不要在前端后台页面使用登录页的星空/粒子特效
8. 所有 API 用相对路径，Vite proxy 转发到 8080
9. `cohortId` 查询参数约定：空=全部、`-1`=未分届、正数=指定届次
10. 届次只能由 fullAccess 通过 `PUT /api/members/{id}` 修改；部门/职务可由本人自改，但禁止自设为管理员身份（秘书处/会长/副会长/部长）
11. 届次显示统一用 `cohortLabel(year)` → "YYYY届"，DB 存完整年份；关系表存多届，禁止逗号字符串
12. 积分项目类型固定 6 类（活动/比赛/开源学习/社区贡献/演讲或主持/其他），后端 `PointItemTypes` 校验、前端 `utils/pointItemTypes.ts` 的 `POINT_ITEM_TYPES` 共享，禁止各页硬编码
13. 删除成员 = 软删除成员 + 清理其积分申请/积分记录（APPLICATION/MANUAL/HOMEWORK 三来源）+ 禁用关联账号；单删/批删共享 `MemberService.deleteMembersInternal`，批量接口 `POST /api/members/batch-delete`

## 项目结构

```
├── pom.xml, docker-compose.yml
├── src/main/java/com/openatom/club/
│   ├── auth/        # 认证（JWT、登录、账号管理、密码）
│   ├── cohort/      # 届次（CRUD、启停）
│   ├── member/      # 成员 CRUD
│   ├── point/       # 积分（项目/申请/记录/总表）
│   ├── finance/     # 财务台账（按月报表+凭据）
│   ├── meeting/     # 会议纪要（文件上传下载）
│   ├── archive/     # 活动资料归档链接
│   ├── file/        # 文件存储服务
│   ├── log/         # 操作日志
│   ├── dashboard/   # 首页统计
│   └── common/      # 配置、异常、响应、安全（ActorHolder/PermissionChecker）
├── src/main/resources/db/migration/  # Flyway V1~V9
└── frontend/src/
    ├── api/         # axios 请求封装 + request.ts 拦截器
    ├── pages/       # 17 个页面组件（含 homework/ 3 个作业页 + 届次管理）
    ├── components/  # AuthGuard, PermissionGuard, LoginStarfield, PageContainer, CohortSelect
    ├── layouts/     # MainLayout（侧边栏+顶栏+用户菜单+改密Modal）
    ├── utils/       # auth, permission, download, actor, cohort
    ├── types/       # TypeScript 类型
    └── styles/      # login.css, app-shell.css, dashboard.css
```
